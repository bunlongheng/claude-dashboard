#!/bin/bash
# core.hooksPath=.githooks would shadow the global secret-guard, so chain to it explicitly.
g="${XDG_CONFIG_HOME:-$HOME/.config}/git/hooks/pre-commit"
[ -x "$g" ] && exec "$g" "$@"
exit 0
