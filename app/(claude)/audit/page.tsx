import type { Metadata } from "next";

export const metadata: Metadata = { title: "Claude | Audit" };

export default function AuditPage() {
    return <p className="text-white/30 text-center py-16">Audit — coming soon</p>;
}
